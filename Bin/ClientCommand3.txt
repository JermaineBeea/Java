import java.util.Map;
import java.util.HashMap;
import java.util.function.Function;

public class ClientCommand3{

    private Position posInstance;
    pricate Map<String, Function<Duuble, Void>> comands = new HashMap<>();

    public ClientCommand3(Position pos){
        this.posInstance = pos;
        comands.put("forward", distance -> {posInstance.moveForward(distance); return null;})
    }

    public void executeCommand(String strCommand, double quantity){
        Function<Double, 
    }
}